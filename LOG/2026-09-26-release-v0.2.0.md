# 2026-09-26 — v0.2.0 릴리스

v0.1.1 이후 main에 들어간 PR #1~#4를 묶은 릴리스입니다.

## 새 기능
- **LLM 에이전트 패널** (#1): OpenRouter / Claude Code / Codex와 대화로 설정을 바꾸고 편집을 제안합니다. 로봇 실행 도구는 없습니다.
- **단계별 선 추출** (#1, #2)
  - 9단계 띠(번호와 결과 한 줄), 큰 보기(확대·이동·원본 겹치기, 이전 단계와 비교)
  - 값을 바꾸면 바뀐 단계부터 자동으로 다시 계산합니다.
  - 색 차이(Lab) 선 검출
- **편집 제안** (#1): 번호로 살리기·지우기·점 편집을 하고, 빨강/초록 제안을 확인한 뒤 적용합니다. 다시 계산해도 편집이 유지됩니다.
- **로봇으로 그리기** (#4)
  - ①~⑥ 안전 절차 창(최종 확인 체크 필수, 공중 모드 기본)
  - 가상 시뮬레이션(로봇 없이), RViz 실시간 따라가기(WSL2 + ROS 2 Humble)
- **새 앱 아이콘** (#3)
- **컬러 원본 표시**, GUI에서 RViz 3D 재생 열기

## 고친 것
- 투명 PNG에서 선이 사라지던 문제
- 기본 100mm 그림이 반올림 오차로 실행기 범위 검사에서 거부되던 문제
- Claude Code 로그인 확인과 로그인 창 열기

## 확인
- 테스트 176개가 CI(Ubuntu/Windows × Python 3.11/3.13)에서 통과합니다.
- 새 가상환경에서 설치하고 exe를 빌드해 확인했습니다: 가상 시뮬레이션 드로잉, 시뮬레이터, MCP 서버, GUI 실행, RViz 모듈 포함.
- 릴리스 CI가 설치판 exe로 가상 시뮬레이션 드로잉까지 검사합니다.
